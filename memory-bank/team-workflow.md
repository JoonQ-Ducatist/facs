# FACS 협업 운영 계약

## 마스터와 작업 기준

- 마스터: `★ PM ★`, thread ID `01a071e5-c4b3-7953-b588-bdcb36f4a2fb`.
- 프로젝트: `창업`, project ID `g-p-6a4395fea4288191ae2538df0ed9ba98`.
- 실제 저장소: `/Users/joonkyou.park/Desktop/impression/facs`.
- 원격 저장소: `JoonQ-Ducatist/facs`.
- 사용자는 마스터에서 관련 작업으로 지시를 배분하고 결과를 회수하도록 승인했다.
- 이 계약은 PM이 실행 중일 때 수행하는 업무 절차다. 백그라운드 감시나
  자동 알림이 별도 설치되었다고 주장하지 않는다.

## 동적 업무 배분

1. 마스터는 새 요청마다 `list_threads`로 실제 존재하는 작업을 확인한다.
2. 아래 초기 등록부는 힌트다. ID의 존재, 현재 제목, 프로젝트, 담당 범위와
   실행 상태를 확인한다. 삭제/보관된 작업에는 보내지 않는다.
3. 새 작업은 프로젝트와 사용자 지정 역할, 필요한 경우 최근 대화를 읽고
   배분 후보에 포함한다. 제목이 비슷하다는 이유만으로 선택하지 않는다.
4. 동일 요청의 진행 작업이 있으면 그 작업에 후속 지시한다. 중복 실행하지 않는다.
5. 사용자가 새 대화방 생성을 요청한 경우에만 새 사용자 대화방을 만든다.
   적합한 작업이 없으면 PM이 처리하거나 한 번의 필요한 역할 질문을 한다.
6. `send_message_to_thread`로 명세를 전달하고 `wait_threads`로 결과를 회수한다.
   실행 중인 작업에 충돌하는 수정 지시를 덧붙이지 않는다.
7. 큰 작업은 독립된 디자인/리뷰/검증 준비를 병렬 배분할 수 있다.
   같은 파일은 한 구현 담당만 수정한다.

## 초기 역할 등록부

| 역할 | 초기 대화방 | 초기 ID | 책임 |
| --- | --- | --- | --- |
| PM | ★ PM ★ | 01a071e5-c4b3-7953-b588-bdcb36f4a2fb | 요구사항, 배분, 통합 판단, 사용자 보고 |
| 설계/리뷰 | (00) 에이전트 세팅 | 01a071ca-d884-7592-8814-8ca28934dfb6 | 협업 규칙, 설계, 독립 코드 리뷰 |
| 구현 | (01) Code 개발 | 01a02d16-041e-72d0-bba2-9ecd742f2c95 | 구현, 테스트, 수정, 문서 동기화 |
| 디자인 | (02) 디자인 시스템 | 01a071cd-7623-7cb2-b610-ccc36875bc20 | 시각적 UI, 컴포넌트, 가독성, 반응형, 상호작용 표현 |
| 서비스 설계 | (03) 서비스 설계 | 01a07427-cdd2-76f0-930b-b419a7050099 | 사용자 여정/동선, 기능 요구사항, 우선순위, 제품 정책, 수익화/현금흐름, 지표 |
| 브라우저 QA | Antigravity | local CLI/IDE | 보고서 전용 독립 검증 |
| 자동 검사 | GitHub CI | .github/workflows/ci.yml | 테스트와 빌드 |

## 전달 계약

각 요청은 `FACS-YYYYMMDD-NNN` 등의 고유 ID를 가지며 아래 내용을 포함한다.

- 목적, 요청 원문 요약, 완료 기준, 제외 범위
- 담당 역할, 실제 저장소 절대 경로, 기준 커밋/브랜치, 기존 dirty 상태
- 수정 소유 파일, 의존 작업, 테스트 모드(mock/staging/production)
- 반환할 증거와 보고 대상 PM ID

담당자는 완료/진행/차단 상태, 변경 파일, 테스트 결과, 미검증 항목,
커밋/PR/미리보기 주소를 반환한다. 결과를 PM에 한 번 전달하고 재배분하지 않는다.
PM은 문제를 재현하거나 증거를 확인한 뒤 수정 담당에게 반환한다.
QA의 PASS는 제품 승인 또는 보안 전체 통과를 뜻하지 않는다.

하위 작업이 완료되면 담당 공간은 결과, 검토할 산출물의 경로·PR·URL, 필요한 사용자
승인 문구를 반드시 `★ PM ★`에 반환한다. 사용자는 하위 공간에서 승인하지 않는다.
PM은 마스터에서 사용자가 확인할 공간과 항목을 안내한 뒤 최종 승인 문구를 받고,
그 기록만 출시·병합의 사용자 승인으로 취급한다.

승인 없이 수행 가능한 작업이 끝나면 PM은 마스터에 완료 사실과 핵심 검증 결과를
짧게 알리고, 승인 게이트를 넘지 않는 다음 순번의 작업을 즉시 시작한다. 사용자
승인은 화면·기능·정책 결정, 외부 계정 권한/자격증명 생성, 데이터 변경, PR 병합과
운영 배포에 필요한 경우에만 요청한다. 외부 대기(DNS 전파 등)는 다음 비차단 작업을
멈추는 사유가 아니다.

서비스 구조, 사용자 동선, 기능 우선순위, 수익화와 지표의 발견은 `(03) 서비스 설계`가
판단한다. 시각적 UI, 컴포넌트, 가독성, 반응형과 상호작용 표현의 발견은 `(02) 디자인
시스템`이 판단한다. 혼합 과제는 `(03)`이 제품 목적과 흐름을 정리하고 `(02)`가 화면
표현으로 구체화하며 `(01)`이 구현한다.

## 검증 및 출시

### 필수 승인 게이트

코드·UI 동작을 변경하는 모든 과제는 아래 순서를 지킨다.

1. PM이 과제 ID, 범위, 완료 기준과 수정 소유자를 확정한다.
2. 구현 담당은 최신 `main`에서 과제 브랜치를 만들고 그 브랜치에서만 수정한다.
3. 구현 담당은 로컬 테스트·빌드를 통과시킨 뒤 브랜치에 커밋·푸시한다.
4. `main` 대상 Draft PR을 만들고 CI 결과와 Vercel Preview URL을 고정한다.
5. Antigravity는 해당 커밋과 Preview URL을 브라우저 QA하고 증거를 반환한다.
6. PM은 CI·QA 결과와 미검증 범위를 사용자에게 보고한다.
7. 사용자와 Antigravity가 모두 승인한 뒤에만 PR을 `main`에 병합한다.
8. 운영 배포의 짧은 smoke test 후 과제를 완료 처리하고 다음 순번을 시작한다.

화면, UI 또는 기능 승인이 필요한 경우에는 로컬, 원격 기능 브랜치 Preview,
`main` 운영 배포 중 해당 URL을 외부 Google Chrome의 새 창 또는 새 탭으로 연다.
Codex 인앱 브라우저나 패널은 사용자 승인 기준으로 사용하지 않는다. 이 Chrome
탭은 사용자와 PM이 함께 확인하는 시각 검토 기준이며, 보고에는 기준 커밋·브랜치와
URL을 함께 남긴다. 사용자가 `★ PM ★`에서 그 정확한 대상의 승인을 기록하기 전에는
다음 구현 과제를 시작하지 않는다.

- `main`에서 직접 구현하거나 승인 전에 병합·운영 배포하지 않는다.
- Antigravity 결과가 `BLOCKED`, `INCOMPLETE`, `STALE_SOURCE`이면 승인으로 보지 않는다.
- Antigravity 세션 종료, 타임아웃 또는 빈 응답은 QA 결과가 아니다. PM은 먼저 CLI
  세션 복구 또는 재인증 필요 여부를 확인하고 최소 응답 probe를 성공시킨 뒤 같은
  커밋·URL·범위로 QA를 재실행한다. 유효한 `PASS`·`FAIL`·`BLOCKED` 보고서와 증거가
  없으면 사용자 시각 검토 요청과 `main` 병합을 진행하지 않는다. 사용자 로그인이나
  외부 상태 변경이 필요한 경우에만 `BLOCKED` 사유와 필요한 조치를 사용자에게 요청한다.
- QA 후 코드가 바뀌면 커밋과 Preview가 달라졌으므로 CI와 영향 범위 QA를 다시 실행한다.
- 사용자의 승인 문구는 반드시 `★ PM ★`에 기록한다. 다른 공간의 의견은 최종 승인으로 대체하지 않는다.
- 문서만 바꾸는 과제도 브랜치와 사용자 검토를 거치되, 실행 화면에 영향이 없으면 Preview·Antigravity QA는 PM이 제외 사유를 기록할 수 있다.
- 운영 장애의 긴급 수정만 사용자의 명시적 승인 후 `main` 직접 반영을 허용하며, 사후 테스트와 기록을 남긴다.
- 승인 또는 병합 전에는 다음 순번의 구현을 시작하지 않는다. 다음 과제의 조사·QA 시나리오 준비만 병렬로 할 수 있다.

### 브랜치와 검증 기준

- 브랜치명: `feat/facs-<task>-<summary>`, `fix/facs-<task>-<summary>`, `chore/facs-<task>-<summary>`.
- 명세 -> 구현/QA 시나리오 준비 -> 브랜치 커밋 고정 -> Draft PR ->
  CI/독립 리뷰/브라우저 QA -> 수정 -> 영향 범위 재검증 -> 사용자 승인 -> main 병합.
- UI 미리보기와 실제 인증/DB 검증을 분리한다. 모의 로그인은 인증 통과가 아니다.
- 결과 정책은 result-contract.md를 따르며 예시의 20표 기준으로 덮어쓰지 않는다.
- 브라우저 QA는 ko/en, 모바일/데스크톱, 실패/빈 상태, 키보드 접근성을 포함한다.
- Chrome 화면 축소를 Safari/실기기 테스트라고 기록하지 않는다.
- Antigravity는 운영 데이터 변경, 결제, 배포, 코드 수정, 정책 변경을 수행하지 않는다.
- Antigravity의 공개 화면 QA는 브라우저 관찰만 수행한다. 로그인, 이메일 전송, 투표
  제출, 파일 업로드, 결제, 운영 데이터 변경은 하지 않는다. 공개 화면 QA는 실제
  인증, 저장, 권한, 집계 검증을 대체하지 않는다.
- 테스트/빌드는 PR 기준으로 실행한다. QA 보고서는 정확한 커밋과 URL에 귀속한다.
- 기능별 브랜치와 `main` PR 흐름은 선택 사항이 아니라 필수다. 자동 병합은 사용하지 않는다.
- QA 명령과 설치 상태는 `memory-bank/antigravity-qa.md`를 참고한다.

### 경량 검증 규칙

품질을 줄이지 않고 중복 검증만 줄인다. 구현 중에는 변경 위험도에 맞춘 검증을
수행하고, `main` 직전 출시 게이트는 생략하지 않는다.

| 변경 유형 | 구현 중 검증 | 출시 게이트 |
| --- | --- | --- |
| 문구·국소 스타일·아이콘 | 변경 화면의 관련 viewport 확인과 diff 점검 | 영향 화면 Chrome 점검, 전체 테스트·빌드 1회 |
| 단일 화면 상호작용 | 관련 단위 테스트와 PC·모바일 핵심 흐름 확인 | 전체 테스트·빌드, Preview QA |
| 인증·권한·DB·결제·배포 | 관련 자동 테스트와 실제 또는 격리 환경의 흐름 확인 | 전체 테스트·빌드, Preview QA, 운영 smoke test |

- 같은 요청에서 나온 작은 UI 수정은 한 번에 반영하고, 수정마다 전체 테스트·빌드·QA를 반복하지 않는다.
- 전체 테스트와 빌드는 브랜치의 사용자 검토 요청 직전에 한 번, QA 수정이 발생했을 때 한 번만 다시 수행한다.
- Antigravity는 기능이 완성된 고정 커밋의 Preview에서 범위 기반으로 한 번 집중 점검한다. 단, 인증·권한·데이터 변경은 브라우저 관찰만으로 통과 처리하지 않는다.
- 대형 전체 화면 캡처와 긴 영상 대신, 문제 지점이 보이는 잘린 캡처를 우선 사용한다.

### Chrome 점검 안내

사용자 시각 점검이 필요한 경우 PM은 Chrome 새 탭을 열기 전에 다음을 마스터에
항상 제시한다.

1. 점검 대상 브랜치·커밋·URL
2. 사용자가 직접 확인할 항목 목록
3. 기대 결과와 승인 또는 재현 보고에 필요한 정보

Chrome 점검 목록은 변경된 기능만 포함한다. 일반적인 화면·기능·접근성 확인을
중복해서 나열하지 않으며, 출시 게이트에 필요한 항목은 빠뜨리지 않는다.
