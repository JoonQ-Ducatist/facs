# FACS 핵심 실행 규칙

이 파일만 매 작업마다 읽는다. 상세 기록은 보관용이며, 출시·권한·외부 환경 변경·
해결되지 않는 예외가 아니면 열지 않는다.

- 저장소: `/Users/joonkyou.park/Desktop/impression/facs`. Docker 없이 기존
  `http://127.0.0.1:4173` 서버와 열린 탭을 재사용한다. 기존 dirty 변경은 보존한다.
- 최근 승인·배포·관련 파일을 한 번만 대조한다. 이미 완료된 동일 항목이면 다음
  미완료 작업으로 이동한다.
- 요청을 UI·기능·운영 오류로 분류하고 정상·실패·복구 조건을 짧게 고정한다.
- 한 작업에는 한 구현 담당자를 둔다. UI와 서버 계약도 한 담당자가 안전하게 소유할
  수 있으면 분리하지 않는다. 병렬 분리는 독립 결과가 실제 시간을 줄일 때만 한다.
- 담당자의 변경·집중 검증 결과를 정본으로 사용한다. PM은 실패·증거 누락·후속 변경이
  없으면 같은 테스트를 다시 실행하지 않는다.
- UI·국소 기능은 관련 테스트 2~5개, 인증·권한·DB 계약은 최대 8개만 실행한다.
  전체 테스트·빌드·전체 화면 점검·반복 Preview는 사용자의 명시 승인 없이는 금지한다.
- 선택 수보다 많은 사례가 든 테스트 파일은 통째로 실행하지 않는다. 이름 필터 또는
  작은 전용 계약 테스트로 좁히고, 실행 전 선택 수를 확인한다.
- 같은 명령·URL·로그·스크린샷·질문은 새 원인·새 산출물·실패가 없으면 반복하지 않는다.
- Gemini는 최신 외부 조사나 실질적 제품 정책 결정에만 쓴다. 구현·오류·QA·테스트·
  Git·배포에는 쓰지 않는다.
- 구현·국소 수정·집중 검증은 즉시 시작한다. DB 적용, 커밋·푸시, `main` 병합,
  운영 배포는 사용자의 해당 승인 또는 기록된 긴급 권한이 필요하다.
- 사용자 점검표에는 번호·점검 방식·정확한 URL을 적고, 실제 인증·저장·업로드는
  모의 화면 확인으로 통과 처리하지 않는다.

역할: `★ PM ★`은 중복 차단·배분·통합·보고, `(01)`은 프론트엔드,
`(02)`는 디자인, `(03)`은 서비스 정책, `(04)`는 백엔드·Supabase 계약,
Antigravity는 읽기 전용 브라우저 QA를 맡는다.

상세 문서가 꼭 필요하면 해당 절만 읽는다:
`memory-bank/team-workflow.md`은 출시·Preview·권한 예외,
`memory-bank/service-design-rule.md`는 제품 정책,
`memory-bank/TECH-AGENTS.md`는 기술·환경 경계에 한정한다.

목표는 품질 저하가 아니라 중복 컨텍스트·중복 작업 분배·중복 검증을 없애는 것이다.
